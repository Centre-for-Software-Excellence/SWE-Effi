---
title: Update Site Data
description: How to update the benchmark & leaderboard data on the site
---

import { Note } from '@/components/md/alerts';

<Note className="mb-12">
  This guide assumes you have cloned the Github repository. All instructions
  below apply to actions performed **inside the cloned repository**.
</Note>

### Leaderboard

##### Table 1

Append new records to `website/src/config/leaderboard/data.json`, each entry should look like:

```json
{
  "scaffold": "Agentless-mini",
  "model": "Qwen3-32B",
  "total": 7438,
  "cpuTime": 0.76,
  "inputToken": 34.1,
  "outputToken": 9.31,
  "calls": 2,
  "infTime": 7437,
  "resolveRate": 28,
  "precision": 48.3
}
```

##### Table 2

Append new records to `website/src/config/leaderboard/data-rvu.json`, each entry should look like:

```json
{
  "scaffold": "Agentless-mini",
  "model": "Qwen3-32B",
  "avgTotalTimeU": 7438.0,
  "avgTotalTimeR": 7438.0,
  "avgCPUTimeU": 0.76,
  "avgCPUTimeR": 0.77,
  "avgInfTimeU": 7437,
  "avgInfTimeR": 7437,
  "avgTotalTokensU": 46.0,
  "avgTotalTokensR": 36.8,
  "avgLLMRequestsU": 2.0,
  "avgLLMRequestsR": 2.0
}
```

No need to run extra scripts—the leaderboard reads this JSON directly.

### Chart pipeline

1. **Add raw stats**  
    Move new benchmark outputs to
   `benchmark/results/agent-scaffold-stats/<scaffold-name>/<model-name>-combined_stats.json`

2. **Copy them into raw data folder** (run from repo root)

```bash
# copies & renames files into website/public/data/benchmark/raw/
python scripts/transform_benchmark.py
```

3. **Generarte chart-ready data**

```bash
cd website
# writes to website/public/data/chart/
npm run generate-data
```

---
